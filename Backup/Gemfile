# frozen_string_literal: true

source "https://rubygems.org"

gem 'github-pages'

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

group :jekyll_plugins do
  # These are from the _config.yml
  gem "jekyll-paginate"
  # These are implicit from my use case
  gem "kramdown"
end

# gem "rails"

gem "jekyll", "~> 3.7"